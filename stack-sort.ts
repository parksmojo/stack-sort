import { Stack } from './stack.ts';

/**
 * Implement this function such that each stack only has one color, 
 * and all of that color, or it is empty. Stack items can only be
 * moved by swapping all of the top color of a stack to another stack
 * who has room, and whose top color matches that of the items to be
 * swapped. For more details, look at the "Nut Sort" game on the 
 * google play store, and the {@link Stack} class.
 * @param stacks The stacks to be sorted
 * @returns The number of moves it took to sort them
 */
export function stackSorter(stacks: Stack[]): number {
  return 0;
}
